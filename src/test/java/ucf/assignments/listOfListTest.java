/*
 *  UCF COP3330 Fall 2021 Assignment 4 Solution
 *  Copyright 2021 David La Rosa Giraud
 */
package ucf.assignments;

//import static org.junit.jupiter.api.Assertions.*;

class listOfListTest {

    @org.junit.jupiter.api.Test
    void addList() {

        /*

        This test will create a list of list objects

        It will create a list object

        Then it will call the addList method

        The test will assert if the list was successfully added
         */

    }

    @org.junit.jupiter.api.Test
    void deleteList() {

        /*
        This test will create a list of list objects

        It will create a list object

        Then it will call the addList method

        It will call the deleteList method

        The test will assert if the list was successfully removed
         */

    }

    @org.junit.jupiter.api.Test
    void getList() {

        /*
        This test will create a list of list objects

        It will create a list object

        Then it will call the addList method

        The test will assert the list returned by calling the getList method
         */

    }

    @org.junit.jupiter.api.Test
    void isEmpty() {

        /*
        This test will create a list of list objects

        It will create a list object

        The test will assert the list of list by calling the isEmpty method

        Then it will call the addList method

        The test will assert the list of list by calling the isEmpty method
         */


    }

    @org.junit.jupiter.api.Test
    void exportOne() {

        /*
        This test will create a list of list objects

        It will create a list object

        Then it will call the addList method

        It will call the exportOne method which create an external file

        it will assert that the file was successfully created
         */
    }

    @org.junit.jupiter.api.Test
    void exportAll() {

        /*
        This test will create a list of list objects

        It will create 2 list object

        Then it will call the addList method twice

        It will call the exportOne method which create an external file inside a loop of listofList.length length

        it will assert that the files were successfully created
         */
    }

    @org.junit.jupiter.api.Test
    void importOne() {

        /*
        This test will create a list of list objects

        It will create a list object

        Then it will call the addList method

        It will call the exportOne method which create an external file

        It will call the deleteList method

        Then it will call the importOne method, having the external file created previously as an argument

        it will assert that the newly added list is the same as the one previously created
         */
    }

    @org.junit.jupiter.api.Test
    void importAll() {

                /*
        This test will create a list of list objects

        It will create 2 list objects

        Then it will call the addList method twice

        It will call the exportOne method twice, which create an external file

        Then it will call the importOne method, having the external file created previously as an argument

        it will assert that the newly added set of list are the same as the one previously created
         */    }
}